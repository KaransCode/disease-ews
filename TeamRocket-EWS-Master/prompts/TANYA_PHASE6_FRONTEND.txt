================================================================================
  TANYA'S PROMPT — PASTE THIS INTO CLAUDE (Phase 6: React Dashboard)
  Time needed: ~10 hours
  Start this after Phase 1. You can build the full UI with MOCK DATA first.
  Connect to real API at integration day.
================================================================================

I am Tanya from Team Rocket, Chandigarh University. We are building the Disease
Outbreak Early Warning System for CODEVISTA v1.0 (Health Tech Track).

MY TASK: Phase 6 — React.js Dashboard + Leaflet Map + Charts

CONTEXT:
- Phase 1 done: create-react-app already set up in frontend/ folder
- Backend API (by Vishal) will run at http://localhost:5000
- The app should work with MOCK DATA until integration day when we connect to real API
- Design style: Clean, professional, light theme with teal (#0094B3) as primary color

BACKEND API ENDPOINTS (build against these — mock them first):
  GET  /api/districts         → [{id, name, lat, lng, score, risk_level, primary_disease}]
  GET  /api/districts/:id/scores  → {district:{}, scores:[{date, score, risk_level}]}
  GET  /api/districts/:id/stats   → {district:{}, stats:[{date, opd_cases, rainfall_mm...}]}
  GET  /api/alerts            → [{id, district_name, sent_at, risk_score, risk_level}]
  GET  /api/scoring/summary   → {districts_monitored, high_risk_count, alerts_sent_today, last_updated}
  POST /api/run-scoring       → triggers ML pipeline

Please give me complete, working code for ALL of these files:

FILE 1 — frontend/src/services/api.js
  - Axios-based API service
  - Base URL from env: process.env.REACT_APP_API_URL || 'http://localhost:5000'
  - Functions: getAllDistricts(), getDistrictScores(id), getDistrictStats(id),
    getAlerts(), getSummary(), runScoring()
  - Each function returns data directly (not the axios response object)
  - Has MOCK_MODE: if API call fails, return realistic mock data for all 22
    Punjab districts so the dashboard works offline during development

FILE 2 — frontend/src/components/StatCards.js
  - 3 cards at top of page:
    * "Districts Monitored" — shows 22, blue teal background
    * "High Risk Zones" — shows count, red background if >0, green if 0
    * "Alerts Sent Today" — shows count, amber/orange
  - Auto-refreshes every 60 seconds
  - Shows "Last updated: X mins ago" below the cards
  - Loading skeleton while fetching

FILE 3 — frontend/src/components/MapDashboard.js
  - Uses react-leaflet with OpenStreetMap tiles
  - Centers on Punjab, India: lat=31.1471, lng=75.3412, zoom=8
  - Shows 22 district markers (CircleMarker) at their lat/lng
  - Color coding:
    * score >= 75 → red (#D0293C), radius=18
    * score 50-74 → amber (#C87000), radius=14
    * score < 50  → green (#1A7A4A), radius=10
    * no data     → gray (#8A99B0), radius=10
  - Clicking a marker calls onDistrictSelect(district) prop
  - Shows popup on hover: district name + score
  - Legend in bottom-right corner showing the 3 risk levels
  - NOTE: Use CircleMarker with lat/lng from API — do NOT use GeoJSON
    (GeoJSON file is hard to source, lat/lng markers work great for demo)

FILE 4 — frontend/src/components/DistrictPanel.js
  - Shown on right side when user clicks a district on the map
  - Shows:
    * District name + state: Punjab
    * Large risk score badge (0-100) in matching risk color
    * Risk level text: LOW / MEDIUM / HIGH
    * Primary disease predicted (Dengue / Malaria / Cholera)
    * Model confidence % (e.g. "87% confident")
    * 14-day case trend line chart (Chart.js) showing opd_cases over time
    * Weather row: rainfall, max temp, humidity for latest day
    * Hospital load progress bar (0-100%)
    * "View full history" button (links to /district/:id — simple route)
  - Shows loading spinner while fetching district data
  - Close button (X) to dismiss panel

FILE 5 — frontend/src/components/AlertBanner.js
  - Fixed banner at top of page (below header)
  - Only visible when there are HIGH risk districts
  - Shows scrolling alerts: "⚠ Ludhiana: Score 82 — HIGH RISK — Dengue surge predicted"
  - Red background (#D0293C), white text
  - Auto-refreshes every 30 seconds

FILE 6 — frontend/src/App.js
  - Main layout:
    * Header: "Disease Outbreak EWS" logo left, "Team Rocket | CODEVISTA v1.0" right
    * AlertBanner below header
    * StatCards row
    * Main content: MapDashboard (left, takes 65% width) + DistrictPanel (right, 35%)
    * Footer: last updated timestamp + "Run Scoring" button
  - State: selectedDistrict (null or district object)
  - "Run Scoring" button calls POST /api/run-scoring, shows loading spinner

FILE 7 — frontend/src/components/SimulateOutbreak.js
  - Button: "Simulate Outbreak — Ludhiana"
  - When clicked, animates Ludhiana's score from 30 → 82 over 3 seconds
  - The map marker changes from green → amber → red with smooth color transition
  - Shows a mock alert banner: "ALERT SENT to CMO Ludhiana — Dengue surge predicted"
  - This is the DEMO MOMENT for judges — make it look impressive

FILE 8 — frontend/src/index.css (or App.css)
  - Clean, professional light theme
  - Primary: #0094B3 (teal)
  - Background: #F4F7FC (soft blue-grey)
  - Cards: white with subtle shadow
  - Font: system font stack (no Google Fonts needed)
  - Responsive for laptop (1280px+)

FILE 9 — frontend/.env
  REACT_APP_API_URL=http://localhost:5000
  REACT_APP_MOCK_MODE=true    ← set to false at integration

After giving me all the code, tell me:
1. npm packages to install beyond create-react-app defaults
   (axios, leaflet, react-leaflet, chart.js, react-chartjs-2)
2. How to run: npm start → should open http://localhost:3000
3. How to switch from mock data to real API (change .env REACT_APP_MOCK_MODE=false)
4. How the SimulateOutbreak component will impress the judges

DESIGN REQUIREMENT: The dashboard must look impressive to judges. Use proper
spacing, a professional color palette, and make the map the visual centrepiece.
It should look like a real government health monitoring tool.

My current Phase 1 status: create-react-app running at localhost:3000 ✅
My OS: [Mac/Windows/Linux]
================================================================================
