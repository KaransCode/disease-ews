================================================================================
  PASTE THIS INTO CLAUDE — EVERYONE RUNS THIS FIRST (Phase 1)
  Time needed: ~20 minutes
  Do this TOGETHER as a team before splitting up
================================================================================

I am [YOUR NAME] from Team Rocket, Chandigarh University. We are building the
Disease Outbreak Early Warning System for CODEVISTA v1.0 (Health Tech Track).

PROJECT OVERVIEW:
- AI-powered web platform that scores Punjab's 22 districts 0-100 for disease
  outbreak risk, 48-72 hours before outbreaks peak
- Stack: Python Flask backend, SQLite database, Scikit-learn + XGBoost ML model,
  React.js + Leaflet Maps frontend, Twilio SMS alerts

PHASE 1 TASK — Project Setup (I need to do this before my specific task):
1. Create the folder structure: disease-ews/{backend/{routes,utils},ml_model,data/{raw,processed},frontend}
2. Create backend/requirements.txt with: flask, flask-cors, pandas, numpy, scikit-learn, xgboost, requests, twilio, APScheduler, joblib, python-dotenv
3. Create backend/schema.sql with 4 tables: districts, daily_stats, risk_scores, alerts
4. Create backend/init_db.py that seeds all 22 Punjab districts with name, population, area, lat, lng coordinates
5. Create backend/app.py — Flask app with CORS, health check at /api/health, blueprint imports commented out
6. Create backend/utils/db.py — SQLite helper with get_db(), query(), execute() functions
7. Create .env.example with: FLASK_ENV, SECRET_KEY, DB_PATH, Twilio credentials, SMTP credentials
8. Create .gitignore

After giving me all files, also give me the exact terminal commands to:
- Create Python virtual environment
- Install all requirements
- Run init_db.py
- Start Flask and verify http://localhost:5000/api/health returns {"status":"ok"}

I am on a [Mac/Windows/Linux] machine.
================================================================================
