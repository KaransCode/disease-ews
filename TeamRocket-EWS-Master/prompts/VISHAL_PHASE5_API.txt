================================================================================
  VISHAL'S PROMPT — PASTE THIS INTO CLAUDE (Phase 5: Flask API + Alerts)
  Time needed: ~8 hours
  Start this after Phase 1. You can build all routes even before Karan's
  model is ready — use mock/demo data first, swap real data in at integration.
================================================================================

I am Vishal Mehta from Team Rocket, Chandigarh University. We are building the
Disease Outbreak Early Warning System for CODEVISTA v1.0 (Health Tech Track).

MY TASK: Phase 5 — Flask API Routes + Alert System

CONTEXT:
- Phase 1 done: Flask skeleton at backend/app.py, SQLite with 22 Punjab
  districts, CORS enabled, utils/db.py working
- Karan is building the ML model (Phase 3+4) — model.pkl will be at ml_model/model.pkl
- Arpit is filling daily_stats with data (Phase 2)
- My job: build all API routes the React frontend calls, plus SMS/email alerts

DATABASE TABLES (read from):
  districts: id, name, population, area_km2, lat, lng
  daily_stats: district_id, date, opd_cases, dengue_cases, malaria_cases,
               cholera_cases, rainfall_mm, temp_max_c, hospital_load
  risk_scores: district_id, date, score, risk_level, primary_disease, confidence
  alerts: id, district_id, sent_at, alert_type, recipient, message, risk_score, status

Please give me complete, working code for ALL of these files:

FILE 1 — backend/routes/districts.py
  Blueprint: districts_bp, url_prefix="/api"

  GET /api/districts
    Returns all 22 districts with today's risk score:
    [{id, name, population, lat, lng, score, risk_level, primary_disease}]
    If no score exists for today, return score=0, risk_level="NO DATA"

  GET /api/districts/<int:district_id>/scores
    Returns last 30 days of scores for one district:
    {district: {id, name}, scores: [{date, score, risk_level, primary_disease}]}

  GET /api/districts/<int:district_id>/stats
    Returns last 14 days of daily_stats for charts:
    {district: {id, name}, stats: [{date, opd_cases, dengue_cases, malaria_cases,
     cholera_cases, rainfall_mm, temp_max_c, hospital_load}]}

FILE 2 — backend/routes/scoring.py
  Blueprint: scoring_bp, url_prefix="/api"

  POST /api/run-scoring
    Triggers the full ML pipeline:
    1. Calls python ml_model/predict.py using subprocess
    2. Returns: {status:"ok", districts_scored:22, high_risk_count:N, timestamp}
    3. After scoring, auto-calls the alert checker
    Handles errors if model.pkl not found (returns helpful message)

  GET /api/scoring/summary
    Returns today's summary stats for the dashboard header cards:
    {districts_monitored:22, high_risk_count:N, medium_risk_count:N,
     alerts_sent_today:N, last_updated:"2026-04-10T06:00:00"}

FILE 3 — backend/routes/alerts_route.py
  Blueprint: alerts_bp, url_prefix="/api"

  GET /api/alerts
    Returns last 10 alerts: [{id, district_name, sent_at, risk_score,
    risk_level, alert_type, status}]

  GET /api/alerts/simulate
    Demo endpoint — simulates a HIGH risk alert for Ludhiana:
    Inserts a fake alert record and returns it (for judge demo, no real SMS)

FILE 4 — backend/alerts.py
  - Function: check_and_send_alerts(district_scores)
    * Takes list of {district_id, district_name, score, risk_level}
    * For any district with score >= 75:
      - Sends SMS via Twilio (reads credentials from .env)
      - Sends email via SMTP/Gmail (reads credentials from .env)
      - Logs alert to SQLite alerts table
    * Has DRY RUN mode: if TWILIO_ACCOUNT_SID starts with "your_", skip real send,
      just log to console (so demo works without real credentials)
    * Returns: {sent: N, failed: N, skipped: N}

FILE 5 — backend/scheduler.py
  - Uses APScheduler to run daily at 6:00 AM:
    1. python backend/ingest.py (fetch fresh data)
    2. python ml_model/predict.py (score all districts)
    3. check_and_send_alerts() (send any needed alerts)
  - Also exposes run_pipeline() function Flask can call manually

FILE 6 — backend/app.py (UPDATED version)
  - Same as Phase 1 but with all blueprints uncommented and registered
  - Imports and registers: districts_bp, scoring_bp, alerts_bp
  - Starts scheduler on app startup

After giving me all the code, tell me:
1. How to test each route with curl commands
2. How to test alerts in DRY RUN mode (no real SMS)
3. What the React frontend expects (so Tanya can connect to my API)

IMPORTANT: Write all routes so they return proper HTTP status codes:
  200 for success, 404 for not found, 500 for server errors.
  All responses must be JSON.

My current Phase 1 status: Flask working at localhost:5000/api/health ✅
My OS: [Mac/Windows/Linux]
================================================================================
